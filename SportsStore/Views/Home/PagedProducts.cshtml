@model ProductsListViewModel

@foreach( var p in Model.Products )
{
    <h3>@p.Name</h3>
    @p.Description
    <h4>@p.Price.ToString("c")</h4>
}

<div>TESTE</div>

<div page-model="@Model.PagingInfo" page-action="pagedproducts">
</div>